# Simulators Directory

Drop your standalone HTML/JS simulator files here.

When you're ready to integrate them into the Next.js app, we'll move them
into `src/app/simulators/[slug]/page.tsx` routes.

This folder is a staging area for your existing code.
